# Networking Concepts for Cloud & DevOps — Deferred

> **Scope note (junior job prep):** This file overlapped the general networking guide and added **cloud/DevOps-specific networking** (VPC, VPN/private connectivity, Kubernetes networking, service mesh) that is **deferred for later study**.
>
> - For the **networking fundamentals** a junior should know (OSI model, TCP/UDP, DNS, HTTP/HTTPS/TLS, ports, proxies, load balancing, CDN, WebSockets), use **`Networking_Interview_Questions.md`**, which is kept in full.
> - The cloud-specific networking (VPC subnets/route tables, VPN/Direct Connect, Kubernetes CNI/Services networking, service mesh like Istio) remains in **git history** — restore it when you move into cloud/DevOps work.

> **Interview soundbite:** "I'm solid on networking fundamentals — OSI layers, TCP vs UDP, DNS, HTTPS/TLS, load balancing. Cloud networking specifics like VPCs and service meshes are things I'd pick up when working in that environment."

---

*Trimmed to a pointer for junior job prep. Restore the full cloud/DevOps networking guide from version control when you're ready to study it.*
